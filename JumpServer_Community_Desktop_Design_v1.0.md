# JumpServer 社区版桌面运维工作台

## 产品需求与技术详细设计

**版本：** v1.0 设计基线  
**日期：** 2026-09-12  
**范围：** 自研终端、SFTP、Picker 与 MySQL GUI CRUD  
**验证状态：** 官方资料与固定源码静态核对；目标部署和测试尚未验证。

---

# 01 方案摘要与使用说明

## 1.1 产品定位

建设一个面向 JumpServer 社区版的自研桌面运维工作台：用户在同一工作空间中，完成资产快速定位、SSH 终端操作、SFTP 文件管理与上传下载、远程文本编辑，以及 MySQL 查询和表格化增删改查。产品不嵌入官方工作台页面，不以唤起外部 SSH 或数据库工具作为核心交付方式。

**推荐基线：Electron + React + TypeScript；终端使用 xterm.js，SQL 与文件编辑使用 Monaco，数据库表格优先评估 Glide Data Grid。自研工作流、状态管理、连接适配与安全边界。** Rust/Tauri 是可替换路线，不作为第一版必须引入的第二套运行时。

JumpServer Core 继续负责身份、资产授权与连接许可；终端和文件能力通过 KoKo 接入，MySQL GUI 通过 Chen 接入。官方架构对这些组件的职责已有说明；官方 V4 Web 终端文档支持 MySQL Web GUI，并将数据库原生客户端及 DB 连接向导标注为 X-Pack。因此，本方案不把原生数据库代理作为社区版的前提。[S01][S02]

## 1.2 已知条件与默认假设

| 项目 | 本文采用的条件 |
| --- | --- |
| 已确认 | JumpServer 社区版；客户端倾向完整自研；Electron 或 Rust 均可；优先追求使用体验 |
| 核心范围 | 美观终端、标签与分屏、SFTP 上传下载与管理、统一 Picker、MySQL GUI CRUD |
| 设计参考 | Terminus/Tabby 的终端组织与分屏；可借鉴 Termius 的资产及文件工作流，但不复制品牌资产 |
| 尚未确认 | 具体 JumpServer/KoKo/Chen 版本、网关部署方式、认证配置、目标操作系统优先级 |
| 服务端变更 | 基线不要求修改服务端；增强能力单独设计，须经管理员确认后部署 |
| 默认目标 | Windows、macOS、Linux 桌面；首个发布平台在项目启动时按实际用户分布确定 |
| 验证状态 | 完成公开文档与固定提交源码的静态核对；未连接用户环境，未执行兼容性或性能测试 |

Terminus 已更名为 Tabby；Termius 是另一个产品。本文将二者作为交互参考，技术实现独立设计。[S22][S23]

## 1.3 四条关键决策

**决策 A：自研的是客户端产品，不是所有基础引擎。** 不从零开发终端模拟器、代码编辑器或 MySQL 协议栈，把工程投入放在上下文联动、可靠状态、输入体验、文件传输和安全写入。

**决策 B：社区版优先使用既有 Web 连接通道。** 桌面 UI 可以完全本地实现；使用 WebSocket 并不等于网页套壳。客户端不获取目标资产的真实密码，不绕过堡垒机直连目标资产。

**决策 C：能力驱动，而不是版本号猜测。** 每项功能同时依赖服务端实现、当前授权、目标账号权限和客户端实现。未知能力按保守策略处理，不能仅凭源码存在某个字段就开放危险功能。

**决策 D：第一版覆盖全部核心工作流。** 终端、SFTP、Picker、MySQL CRUD 都属于核心版。若目标 Chen 不满足安全 CRUD 前提，应明确作为发布阻塞项，选择兼容版本或获批扩展；“只能查询”的兼容模式不能宣称完整满足需求。

## 1.4 本文的事实与设计边界

文中以 [Sxx] 标记外部事实来源。其余标为“建议”“默认”“目标”“拟议”的内容属于本项目设计，不代表 JumpServer 已有能力。所有 TypeScript 类型、内部接口、错误码和增强协议均为设计契约，不能直接当作官方 API 调用。

源码核对采用固定提交，详见附录 C。固定提交只用于说明所观察到的行为，不构成稳定发行版或跨版本兼容承诺。依赖版本在开发启动时锁定到经过安全审查与验证的受支持版本，不在本文臆造“最新版本号”。

# 02 需求范围与用户任务

## 2.1 典型使用任务

**任务一：定位故障。** 用户打开 Picker，输入资产名或地址片段，切换到已有连接或建立新连接；终端侧栏始终显示环境、资产和账号。用户分屏比较日志，打开同一资产的文件面板，下载选定日志，全程不重复选择资产。

**任务二：修改配置。** 用户从文件面板打开文本文件，在编辑器中修改，查看差异并保存；若远端发生变化，先处理冲突。保存文件不自动触发重启服务，相关命令只能由用户明确执行。

**任务三：修复数据库记录。** 用户在 Picker 搜索表名，查看带数据库上下文的结果；打开表后筛选记录，编辑单元格、新增或标记删除，预览变更并确认提交。异常时明确区分未提交、已回滚和提交结果未知。

**任务四：日常批量传输。** 用户拖入文件或目录，选择冲突策略，查看进度、速度和失败原因；关闭面板不必终止已确认的任务，注销或授权失效则停止对应任务。

## 2.2 功能需求与验收标识

| 编号 | 核心需求 | 完成定义 |
| --- | --- | --- |
| R-A01 | 登录与身份隔离 | 支持目标环境实际启用的认证；不同站点、用户的令牌与缓存不混用 |
| R-A02 | 授权资产与账号选择 | 只展示当前用户可访问资产；连接前重新确认账号与连接方式 |
| R-W01 | 标签、分屏、布局恢复 | 切换不重新连接；恢复布局不自动重放命令或恢复过期授权 |
| R-T01 | 可用且美观的终端 | 中文输入、粘贴、窗口缩放、全屏应用、搜索与快捷键行为正确 |
| R-F01 | SFTP 文件管理 | 浏览、上传、下载、建目录、重命名、删除形成完整闭环 |
| R-F02 | 传输任务管理 | 多任务有进度、取消和清晰失败状态；大文件不全量装入内存 |
| R-F03 | 远程文件编辑 | 文本打开、修改、差异预览、保存；不静默覆盖检测到的远端变化 |
| R-P01 | 全局 Picker | 跨资产、标签、路径、数据库表与应用命令搜索，支持键盘完成操作 |
| R-D01 | MySQL 查询工作台 | 元数据树、SQL 编辑、执行、取消、结果和错误展示 |
| R-D02 | MySQL 表格 CRUD | 单表新增、修改、删除、预览、提交；主键和精确类型处理正确 |
| R-S01 | 保留服务端管控 | 当前授权、命令控制、连接终止及审计在目标环境中实测有效 |

## 2.3 核心版、增强版与非目标

| 范围 | 内容 |
| --- | --- |
| 核心版必须有 | 上表所有需求；其中危险操作以已验证能力和权限为前提 |
| 增强能力 | 真正断点续传、原子条件保存、终端目录自动跟随、高级事务会话、大型结果导出、远程到远程传输 |
| 第一版非目标 | RDP/VNC、Kubernetes 管理、多种数据库、云端同步、插件市场、AI 自动执行、数据库结构设计器 |
| 默认关闭 | 广播输入、自动执行片段、后台递归扫描全盘、终端完整输入历史、自动恢复数据库写入 |

“增强”不意味着基本文件管理或 CRUD 被推迟。它指需要额外服务端保证的可靠性能力，以及超出核心任务的高级操作。对于无法安全降级的核心写入功能，必须阻止发布或明确限定兼容范围。

## 2.4 最小贯通切片

第一个可演示版本不是一张漂亮首页，而是：登录 → Picker 选择一台授权资产 → 打开终端 → 打开该资产文件面板并上传小文件 → 打开一个测试 MySQL 表 → 完成一次经确认的单行修改 → 核对审计记录。界面和协议团队以这个闭环作为共同目标。

# 03 产品信息架构与视觉规范

## 3.1 工作台布局

采用“窄导航栏 + 可折叠资源侧栏 + 中央标签/分屏 + 底部任务区”。默认布局以工作内容为中心，不使用大量统计卡片和表单占据首屏。

| 区域 | 默认建议 | 职责 |
| --- | --- | --- |
| 全局导航 | 48 px 宽 | 资产、文件、数据库、任务、设置 |
| 资源侧栏 | 240 px，可调 | 资产分组、收藏、当前数据库元数据树 |
| 上下文栏 | 40 px 高 | 站点、环境、资产、账号、连接状态 |
| 标签栏 | 36 px 高 | 标签切换、固定、分组、未保存标识 |
| 内容区 | 弹性占满 | 终端、文件双栏、SQL 编辑器或结果网格 |
| 任务面板 | 默认收起 | 传输队列、执行错误、取消结果和活动任务 |
| 状态栏 | 24 px 高 | 当前焦点对象、连接状态、编码和任务摘要 |

窗口最小可用宽度建议 1024 px；宽度不足时折叠侧栏，而不是挤压终端到不可用。所有尺寸是初始设计值，需通过原型测试调整。

## 3.2 建议设计令牌

| 令牌 | 深色初始值 | 使用说明 |
| --- | --- | --- |
| canvas | #0F141B | 主画布，不使用纯黑作为所有区域背景 |
| surface | #161D27 | 侧栏与编辑器辅助面板 |
| raised | #1E2937 | Picker、菜单、浮层 |
| border | #344154 | 区域分隔；焦点不得仅靠普通边框表示 |
| text-primary | #E6EDF3 | 正文与主要操作 |
| text-secondary | #A6B3C3 | 路径、辅助信息 |
| accent | #70A5FF | 当前焦点、选中项与主要按钮 |
| danger | #FF7B86 | 破坏性操作；同时显示图标与文字 |
| success | #7BDCB5 | 已完成状态；不得仅用颜色传达结果 |

采用系统 UI 字体；终端提供等宽字体选择和字体回退。正文建议 13–14 px，终端默认 14 px、行高约 1.2–1.3，网格行高 28–32 px。禁止为追求紧凑而省略目标账号或环境名称。普通正文对比度目标至少 4.5:1，并对所有交互状态实测，而非仅检查默认颜色。[S29]

## 3.3 三种工作区

**终端工作区：** 中央终端占主要面积，右侧按需展开当前资产文件面板；标签标题包含资产短名和账号，生产环境采用文字徽标。分屏焦点边框与输入目标始终明确。

**文件工作区：** 侧边面板适合顺手操作；完整视图采用本地/远程双栏。列表显示名称、大小、修改时间、权限及链接类型。操作工具栏根据当前权限和选中对象变化。

**数据库工作区：** 左侧库表树；中央为 SQL 编辑器或表数据；底部显示结果与消息；右侧提供当前行详情及 JSON/长文本编辑。结果集和表格编辑标签必须在视觉上区分，避免用户误以为任意查询结果都可写回。

## 3.4 一致交互规则

所有菜单、按钮、快捷键和 Picker 调用同一套命令注册表。危险动作名称使用“删除远程文件”“提交数据库变更”等明确文案，不使用含糊的“确定”。取消操作应保留用户上下文；关闭弹窗后焦点返回发起控件。动画以 100–150 ms 为初始目标，提供减少动态效果模式。

资产别名、颜色和分组可以本地个性化，但不能覆盖真实资产标识。展示别名时仍可查看真实名称、地址和账号。离线缓存中的资产要明确标为缓存数据，不代表仍有访问权限。

# 04 技术选型与决策记录

## 4.1 Electron 与 Tauri 的取舍

| 维度 | Electron 基线 | Tauri / Rust 备选 |
| --- | --- | --- |
| 渲染环境 | 随应用提供 Chromium，便于锁定测试环境 | 使用系统 WebView，需覆盖平台实现差异 |
| 客户端执行层 | Node.js / TypeScript，可与前端共享类型 | Rust 异步任务与类型系统，前端通过命令/事件通信 |
| 本项目首要收益 | 减少终端、中文输入和复杂表格的渲染变量 | 适合已熟悉 Rust、重视原生执行层的团队 |
| 首版主要成本 | 进程和内存预算、Electron 安全更新 | WebView 差异、Rust 工程与跨语言调试 |
| 决策 | 无既定团队偏好时首选 | 团队已有成熟 Rust 能力时直接选择，不必先做 Electron |

Electron 和 Tauri 的进程/渲染方式可参考各自官方文档；本表的优先级判断属于针对本项目的工程建议，不是通用性能结论。[S10][S12]

## 4.2 建议组件组合

| 层次 | 建议选择 | 需要自研的部分 |
| --- | --- | --- |
| 工作台 UI | React + TypeScript | 导航、上下文、布局、焦点与编辑状态 |
| 终端 | @xterm/xterm 及官方插件 | KoKo 适配、实例管理、输入策略和背压 |
| 文本/SQL 编辑器 | Monaco Editor | SQL 元数据补全、诊断、远程保存、草稿恢复 |
| 数据表格 | Glide Data Grid，先做原型评估 | 精确类型、服务端分页、变更集和写入语义 |
| 资源列表 | 虚拟列表 + 自定义行组件 | 权限状态、上下文菜单与搜索 |
| 本地元数据 | SQLite | 命名空间、迁移、清理和加密策略 |
| 协议执行 | utility process | 认证作用域、KoKo/Chen 消息适配、任务调度 |
| 运行时校验 | JSON Schema 或同类校验库 | 每条 IPC、响应和消息的边界约束 |

Monaco 是浏览器代码编辑器，不等于完整 VS Code，也不自动提供数据库连接或 SQL 智能服务。Glide 提供网格编辑基础，但排序、过滤和数据源集成仍需自行实现。[S24][S25]

## 4.3 明确不采用的方案

不将官方页面放入 iframe 作为核心工作区；不通过终端运行 mysql 并解析字符表格；不在客户端保存管理员密钥或目标资产密码；不为方便跨域关闭 webSecurity；不在第一版同时引入 Electron、Rust sidecar 和本地 HTTP 服务三套通信链路。

若后续分析证明某个执行任务确实受限，再把该模块抽为 Rust 组件。跨运行时边界仍使用同一套类型化协议，UI 不感知实现语言。高频数据不得以“用了 Rust”代替真实性能分析。

## 4.4 依赖和维护原则

锁定运行时与依赖版本，维护依赖清单和许可证记录，打包时保留适用通知。使用官方源码作为参考不等于可以任意复制整个组件；若复制或修改代码，按对应固定版本的许可证处理，并进行发布审查。此处不对具体分发方式作法律结论。

# 05 总体架构与进程职责

## 5.1 分层结构

```text
本地 UI Renderer
  Workspace / Picker / Terminal / Files / Database
                     |
          受限控制 IPC + 数据 MessagePort
                     |
Desktop Main                  Utility Process
  Window / AuthBroker         SessionManager
  OS Dialog / Safe Storage    KoKo / Chen Adapters
  Policy / Update             Transfer / Query Tasks
                     |
            JumpServer 网关（HTTPS / WSS）
                     |
             Core / KoKo / Chen
                     |
           已授权 SSH 资产 / MySQL
```

这是本项目建议架构。Electron 官方支持 utility process 与 MessagePort 等进程通信方式；消息是否产生复制、吞吐上限是多少，必须在选定 Electron 版本上测量，不承诺天然零拷贝。[S10]

## 5.2 各层责任

| 模块 | 负责 | 不负责 |
| --- | --- | --- |
| Renderer | 展示、交互、本地未提交编辑、焦点 | 凭据持久化、任意文件访问、直接连接目标资产 |
| Main | 登录窗口、身份隔离、系统对话框、受限凭据分发 | 执行大文件循环、解析海量结果、长期阻塞任务 |
| Utility | 网络会话、协议适配、有界缓冲、文件流、查询状态 | 接受来自 UI 的任意 URL 或任意本地路径 |
| Adapter | 版本差异、协议字段、错误映射、能力声明 | 决定产品页面布局或吞掉服务端拒绝信息 |
| 服务端 | 身份校验、目标连接、最终权限、审计与终止 | 信任客户端传来的授权结论 |

每个进程崩溃都要有明确恢复边界。Utility 崩溃会导致其连接不可继续使用；恢复布局和草稿，但不得自动重放输入、上传覆盖或数据库写入。第一版可以一个 utility 承载有限会话；达到配额后拒绝新任务或经验证地分片，不无限创建进程。

## 5.3 状态所有权

工作空间状态属于 UI；有效连接和任务状态属于 SessionManager；认证属于 AuthBroker；数据库已提交结果以服务端为准。UI 的 loading 不是真实执行状态，关闭组件不代表服务端操作已取消。

终端原始输出、大文件块和完整结果集不进入通用 React Store。Store 只保存状态摘要、引用和视图选择。SQL/文件编辑器模型由独立 registry 管理，组件重渲染或标签隐藏不应销毁模型。

## 5.4 资源治理

设置单站点、单用户和单会话的连接数、任务数、结果缓存和输出缓冲上限。控制消息优先于大文件数据；心跳、终止、取消不能排在长查询或大传输之后。达到上限时采取可解释的限流或停止，不静默丢弃终端输出和数据库数据。

# 06 登录、身份与连接许可

## 6.1 认证接入策略

优先在隔离的登录窗口中完成目标 JumpServer 实际启用的认证，复用其 MFA 或重定向流程；登录窗口不加载本地工作台的高权限 preload。主进程确认登录成功后，仅向业务层提供身份摘要和受限会话句柄。

JumpServer API 文档列出 Session、Token、Private Token、Access Key 等方式，并提供部署实例的 /api/docs/ 入口。[S03] 本项目不把某一种认证方式假设为所有部署必然可用，不假定社区版具有所有企业 SSO 功能，也不假定外部浏览器的 Cookie 可以自动共享给桌面端。

若部署要求系统浏览器认证，必须使用该部署支持的安全回调或票据交换机制；不存在受支持的交换流程时，不抓取浏览器凭据、不伪造登录态，而将接入限制记录为待解决兼容项。

## 6.2 认证对象必须分开建模

| 对象 | 用途 | 安全处理 |
| --- | --- | --- |
| 登录 Cookie / API 凭据 | 访问 Core 授权接口 | 仅放入对应站点、用户作用域；不写入普通日志 |
| Core 连接令牌 | 许可一次具体资产连接流程 | 区分 token id 与 secret；具体寿命和重用规则按版本处理 |
| KoKo 连接票据 | 特定版本的 Web 连接握手 | 由适配器管理，不猜测所有版本都需要或支持 |
| Chen 会话令牌 | Chen 内的数据库会话 | 不当作 MySQL 密码，不混用于其他资产 |
| 数据库查询句柄 | 某个会话中的控制台/查询 | 绑定 generation，避免旧响应影响新连接 |

源码中可观察到 Core 连接令牌 API；Chen 的认证与 WebSocket 会话又有独立生命周期。固定提交的 Chen 握手会核对 Origin、子协议携带的会话令牌，以及创建令牌时的 HTTP session 绑定。[S04][S09][S31]

## 6.3 AuthBroker 的职责

AuthBroker 按 siteId + userId 隔离网络会话。组织上下文作为请求参数/头部的一部分传递；只有部署支持多个组织时才展示切换入口，否则使用服务端返回的默认上下文，不能硬编码网上示例中的组织 UUID。

主进程内的 Chromium 网络会话与 utility 内的 WebSocket 客户端不会因为“同属一个应用”就自然共享完整登录状态。应采用经过验证的网络桥接方案：HTTP 通过指定会话执行；WebSocket 按站点允许列表获得最小必要的 Cookie/票据上下文，并正确处理更新与过期。Electron 的 net 使用 Chromium 网络栈，但其具体 Session 和 Cookie 选项仍需显式配置。[S16]

不将完整 Cookie jar 暴露给 renderer，不提供可请求任意域名的凭据代理。重定向时重新验证目标；目标地址超出管理员配置的入口或组件允许列表则停止。不能通过伪造来源绕过服务端策略；不兼容的 Origin 策略需通过受支持配置或批准的服务端改动解决。

## 6.4 登录与连接状态机

```text
SignedOut -> Authenticating -> Ready
                   |           |
                   v           v
                 Failed     ReauthRequired
                               |
                              Ready
Ready -> SigningOut -> SignedOut
```

用户注销时先阻止新任务，再关闭对应终端、文件和数据库会话，清理临时令牌及敏感缓存。即使底层请求无法立刻取消，也不再向已注销 UI 交付数据，并将未确认操作标记为结果未知。不要把断开本地连接等同于确认服务端已经回滚。

## 6.5 安全持久化与诊断

默认不保存密码和长期 API 密钥。确需记住登录时，使用系统安全存储并检测可用性。Electron 文档说明 Linux 某些环境会退化到 basic_text；检测到不满足安全策略的后端时，禁用敏感凭据持久化，而不是继续显示“已安全加密”。[S15]

诊断包记录应用版本、适配器、状态转换、脱敏错误及时间信息；不包含 Cookie、令牌、完整 SQL、文件正文或终端输出。凭据处理的每个失败都必须返回明确错误，不使用“忽略证书错误”作为常规修复建议。

# 07 JumpServer 协议适配与兼容矩阵

## 7.1 可核对的协议入口

下表来自官方文档或已核对的固定源码提交，仅表示接入线索。网关前缀、参数、权限、返回结构和生命周期必须以目标部署验证结果为准；不能复制路径后自行编造请求体。

| 组件 | 已观察的入口或动作 | 参考 |
| --- | --- | --- |
| Core | GET /api/v1/perms/users/self/assets/ | [S04] |
| Core | 授权资产树相关 self/nodes 接口 | [S04] |
| Core | POST /api/v1/authentication/connection-token/ | [S04] |
| KoKo | /koko/ws/terminal/ | [S05] |
| KoKo | /koko/ws/sftp/；兼容分支可有 elFinder 路径 | [S05] |
| KoKo | /koko/api/connect-ticket/ | [S05] |
| Chen | 组件内 /api/auth；/ws/session 与 /ws/console | [S09][S31] |
| Chen | 结构化表格预览/保存；新增、修改和删除请求字段 | [S07][S08] |

Chen 的外部路径通常经过网关前缀，不能把组件内路径直接视为完整外部地址。每个适配器负责将 site base path 与组件路由组合，保留部署子路径，防止路径被覆盖或重复拼接。

## 7.2 连接序列

**终端：** 确认登录 → 查询授权资产和账号 → 获取该连接方式许可 → 按版本申请票据 → WSS 握手 → 完成终端初始化 → 建立输入/输出/resize 通道 → 接收关闭和审计相关控制消息。

**SFTP：** 从终端上下文复用资产与账号选择，不默认复用同一个远端会话或令牌 → 根据授权建立独立文件会话 → 读取文件能力 → 列目录 → 执行操作 → 关闭或随授权结束。终端和文件面板共享业务上下文，不等于共享一个底层连接。

**MySQL：** 获取数据库资产连接许可 → Chen 认证创建会话 → 保持所需 HTTP 会话 → 建立主会话通道 → 建立查询/数据视图控制台 → 获取元数据和数据 → 执行、取消、处理消息 → 明确关闭控制台与会话。

## 7.3 适配策略

为首个实际部署建立 jms-<validated-profile> 适配器，保存脱敏握手、初始化和消息样本。对后续兼容版本增加独立 profile，而不是把所有差异写成 UI 中的 if/else。未经验证的版本进入“有限兼容”状态，逐项标记能力，不自动宣称全面支持。

能力状态必须区分 supported、unsupported、unknown。每条能力记录来源、核对时间、限制和原因。能力显示与授权最终判断分离：客户端显示可用仍不替代服务端再次校验。

```typescript
// 本项目内部模型，不是 JumpServer 官方响应。
type Support = 'supported' | 'unsupported' | 'unknown';
interface Capability {
  state: Support;
  evidence: 'declared' | 'validated-profile' | 'probe' | 'none';
  reason?: string;
  limits?: Record<string, string | number | boolean>;
}
interface ConnectionCapabilities {
  terminal: Capability;
  files: Record<string, Capability>;
  database: Record<string, Capability>;
}
```

## 7.4 兼容矩阵模板

| 能力 | 默认基线 | 缺失时处理 |
| --- | --- | --- |
| 终端交互 | 必须验证 | 无法通过则不支持该部署 |
| 文件浏览与基本上传下载 | 必须验证 | 不降级为解析 shell 输出 |
| 文件删除/重命名/建目录 | 按协议及权限验证 | 显示原因，禁止改用隐藏 shell 操作 |
| 断点续传 | unknown | 仅提供明确的从头重试 |
| 条件文件保存 | unknown | 提示只能尽力检测，或要求增强后启用安全保存 |
| 查询与取消 | 必须验证 | 取消不能确认时显示结果状态未确认 |
| 表格 CRUD | 必须验证 | 不满足时阻塞完整核心版验收 |
| 联合主键/事务批次 | unknown | 不安全场景只读或要求增强适配 |

探测只使用无副作用或显式授权的测试操作；不得通过删除、覆盖文件或执行写入来偷偷检测能力。上线前用隔离测试资产做完整验证，并将结果加入签名发布包内的兼容清单。

# 08 工作空间、会话与消息契约

## 8.1 工作空间模型

工作空间保存布局和资源引用，不保存可复用的目标密码。标签由 tabId 标识，底层连接由 sessionId 标识，一个标签可被移动或分屏呈现，但不能因此重复建立连接或重复订阅输出。

```typescript
// 内部设计类型；字段需在运行时边界再次校验。
interface ResourceContext {
  siteId: string;
  userId: string;
  orgId: string;
  assetId: string;
  accountId: string;
  protocol: 'ssh' | 'sftp' | 'mysql';
}
interface SessionRef {
  sessionId: string;
  generation: number;
  context: ResourceContext;
}
type SplitNode =
  | { kind: 'leaf'; tabId: string }
  | { kind: 'split'; axis: 'horizontal' | 'vertical';
      ratio: number; children: [SplitNode, SplitNode] };
```

重新连接时 generation 增加；旧请求和旧数据即使晚到，也不能更新新会话。不同用户的缓存键必须包含完整身份作用域；名称相同不能当作同一资产。

## 8.2 会话生命周期

```text
Idle -> Authorizing -> Connecting -> Initializing -> Active
            |              |              |          |
          Failed         Failed         Failed    Closing
                                                    |
                                               Closed / Lost
```

Lost 表示传输丢失，不代表远端命令、上传或事务已经结束。恢复按钮的语义是“建立新连接并恢复界面上下文”，不是重播刚才的输入。使用 tmux 等远端会话保持工具属于用户可选工作流，客户端不默认替用户执行安装或配置。

## 8.3 控制面与数据面

控制消息包括建立连接、关闭、resize、取消和状态订阅；数据面承载终端字节、文件块、查询数据批次。两者隔离队列或设置优先级。所有请求有 requestId、会话代际、截止时间与取消语义。

```typescript
interface RequestEnvelope<T> {
  requestId: string;
  session: SessionRef;
  method: string;
  payload: T;
  deadlineMs: number;
}
interface AppError {
  code: string;
  message: string;
  retry: 'safe' | 'after-confirmation' | 'never';
  outcome: 'not-started' | 'unchanged' | 'changed' | 'unknown';
  correlationId: string;
}
```

这些是进程间设计模型，不应直接允许 renderer 任意指定 method。主进程按白名单分发，并验证 webContents、资源句柄和请求所属会话。UI 传入的 userId/orgId 只能作为一致性检查，实际身份取自已验证会话。

## 8.4 关闭和恢复规则

关闭标签时，如果存在未保存文件、未提交表格编辑或正在执行 SQL，显示对象级提醒。关闭文件面板但继续传输是允许的明确选择；退出应用或注销必须停止接收新任务并告知未完成操作。

布局恢复可以自动进行；资产连接、终端内容持久化、数据库草稿恢复分别遵守用户设置和安全策略。即使草稿自动恢复，也必须重新确认目标资产，防止把旧环境的 SQL 提交到新环境。

# 09 终端详细设计

## 9.1 终端实例与渲染

每个有效终端连接对应稳定的 xterm 实例。标签隐藏或侧栏开关不销毁实例；调整尺寸在容器稳定后计算行列数并去重发送。切换显示器、缩放比例、字体和输入法后应重新测量；为渲染加速失败提供可用回退，不让 GPU 故障导致整页不可操作。

默认保留有上限的本地回滚缓冲。搜索只覆盖当前可用缓冲，不宣称搜索远端全部历史。需要导出终端内容时作为独立操作处理，并受企业策略和敏感数据规则约束。

## 9.2 输入处理

输入路径是用户事件 → 终端模拟器编码 → 版本适配器封装 → KoKo。不要用普通文本框模拟终端输入。正确处理中文 composition、组合键、鼠标模式、粘贴换行及应用光标模式。

| 场景 | 必须行为 |
| --- | --- |
| Ctrl+C | 在终端中传递中断输入；不能同时当作无条件复制 |
| Ctrl+S / Ctrl+Q | 终端焦点下保留终端语义，应用保存快捷键不拦截 |
| 多行粘贴 | 显示预览和换行提醒；用户明确确认后再发送 |
| 快捷命令 | 默认插入/预览，不自动回车执行；不存敏感参数 |
| Picker 关闭 | 焦点回到原终端或发起控件，不切换输入目标 |
| 广播输入 | 核心版默认禁用；增强时必须持续显示目标列表 |

向 PTY 发送 Ctrl+C 不是强制杀死远端进程。界面应使用“发送中断”而不是保证“命令已终止”。重连后绝不重新发送尚未确认执行的键盘缓冲。

## 9.3 输出、背压和限流

输出直接写入有界缓冲，再交由 xterm.write 处理，不逐块进入 React 状态。利用写入完成信号参与客户端流控；端到端背压还需要 KoKo 通道配合。xterm 官方文档指出，终端缓冲与远端输出速度不匹配会造成问题，不能只依赖网络能接收数据。[S13]

设定每会话缓冲高低水位；达到高水位时优先请求受支持的流控或降低读取速率。底层无法背压且内存风险上升时，采用明确提示并受控断开，而不是悄悄丢掉部分输出。转储到磁盘只作为用户或企业明确允许的模式，设置容量和清理规则。

## 9.4 终端内容安全

远端输出、窗口标题、OSC 超链接和剪贴板控制序列均视为不可信。标题去除控制字符并限长；点击外链先验证协议和目的地；未经授权不允许远端序列写入系统剪贴板。不可将终端输出作为 HTML 执行。xterm 官方安全指南同样要求把终端内容当作潜在恶意输入处理。[S14]

## 9.5 目录联动

基础联动是文件面板将经过校验的路径复制或插入到终端，默认不自动执行。只有已知 shell 类型时，才使用对应的安全引用策略生成 cd 命令；包含换行或控制字符的路径应拒绝直接插入，未知 shell 只提供复制。

高级联动通过可选 shell integration 报告当前目录，不解析提示符猜路径。可参考 VS Code 的 shell integration 控制序列设计，但需要验证 KoKo 透传、shell 配置和路径映射。[S26] 不静默改写远端启动文件；sudo、容器、chroot 等上下文无法映射到当前 SFTP 根目录时，停止自动跟随并说明原因。

# 10 SFTP 文件管理与传输

## 10.1 文件 API 与视图模型

文件视图使用统一内部接口：list、stat、mkdir、rename、remove、openRead、openWrite；具体动作由 KoKoFileAdapter 映射。当前核对的 KoKo 源码包含 list/download/upload/rm/rename/mkdir 以及保存和传输动作，但这些能力必须按目标版本验证。[S06]

文件项保留 name、canonicalPath、type、size、mtime、permissions 和可选版本信息。size 使用精确整数表示；远端路径按远端文件系统语义处理，不使用 Windows 本地路径函数拼接 POSIX 路径。文件名显示与实际路径分离，界面缩略不改变真实操作对象。

## 10.2 文件操作规则

| 操作 | 交互要求 | 安全/一致性要求 |
| --- | --- | --- |
| 浏览 | 面包屑、路径输入、前后跳转、隐藏文件 | 访问拒绝与不存在分别展示；不靠解析 ls |
| 上传 | 拖入、选择文件/目录、目标确认 | 同名默认询问；保留原文件直到明确覆盖 |
| 下载 | 系统文件选择器、进度、完成定位 | 本地路径由系统授权句柄派生，不信任远端文件名 |
| 重命名 | 原名和新名可见 | 校验目标冲突、大小写规则和路径范围 |
| 删除 | 明确数量、路径、是否目录 | 不假设远端有回收站；递归删除单独确认 |
| 属性 | 类型、权限和链接信息 | 不支持 chmod 时明确禁用，不暗中走 shell |

远端符号链接默认不递归跟随；批量目录下载需检测环路、数量和容量上限。下载到本地时防止路径穿越、绝对路径覆盖和压缩包目录逃逸；遇到本地不允许的文件名，不静默映射到可能冲突的路径。

## 10.3 任务引擎

任务独立于文件面板，绑定完整身份和资源上下文。初始建议每资产最多 2 个并发传输，全局最多 4 个；文件块初始 256 KiB–1 MiB 范围按网关和协议限制协商。这些是性能起点，不是服务端已支持的固定参数。

```text
Queued -> Preparing -> Transferring -> Verifying -> Committing
                            |                         |
                         Canceling                 Completed
                            |
                         Canceled
任何执行阶段 -> Failed / OutcomeUnknown
```

传输从磁盘流式读取和写入，UI 仅接收节流后的进度。吞吐和剩余时间按滑动窗口估算；未知总大小时显示已传输量，不显示虚假的百分比。零字节文件必须单独测试。

## 10.4 重试与断点续传

只读目录请求可有限重试；写入不能不加判断地重新发送。普通上传断线后重新开始必须明确告知用户。只有服务端具备传输 ID、偏移状态、绑定身份、内容校验和完成提交语义时，才能称为断点续传。

当前固定提交出现 transfer_prepare、transfer_write、transfer_status、transfer_commit、transfer_cancel 等动作，但客户端不能仅据此推断已经具备完整可恢复保证。[S06] 适配前需测试：断线后状态是否保留、是否允许重新认证恢复、偏移是否可信、目标是否变化、终止操作是否清理临时文件。

断点恢复时校验源文件标识、大小与修改状态；服务器确认的持久化偏移才是恢复依据。不能只使用客户端“已发送字节数”。同一用户的新令牌是否可继承旧任务由服务端决定，不由客户端绕过。

## 10.5 完成语义和权限

“客户端发完”不等于“服务器完成”。最终成功要求收到对应提交/完成确认；增强通道可再校验大小和摘要。基础协议无法提供摘要时，不显示“内容校验通过”。原子 rename 只在经验证的相同文件系统与权限条件下使用，不能通用承诺原子替换。

终端里执行过 sudo 不会自动改变文件面板使用的账号。UI 展示实际 SFTP 身份；权限不足时提示重新选择被授权账号，不自动通过终端提权绕过文件权限。注销、服务端终止或授权失效时停止新块发送，并如实说明远端已完成部分可能存在。

# 11 远程文件编辑与冲突处理

## 11.1 编辑器生命周期

打开文件时获取内容、编码、换行风格、权限和版本信息，创建稳定的 Monaco model。默认仅打开大小受控的文本文件；建议文本编辑上限初值 5 MiB，并允许管理员收紧。二进制或无法可靠解码的内容进入只读预览，不进行自动替换字符后的覆盖保存。

标签状态区分 clean、dirty、saving、conflict、save-unknown。保存成功后更新基线版本；保存中再次编辑的内容属于下一次变更，不能被前一次响应覆盖。不同标签打开同一远端文件时，复用编辑会话或显示重复编辑警告。

## 11.2 安全保存流程

```text
打开文件并记录基线版本
        -> 本地编辑 -> 查看差异 -> 请求保存
                                  |
                       条件匹配？--+--否 -> 冲突处理
                                  |
                                  是 -> 保存确认 -> 更新基线
```

当前 KoKo 固定源码包含 expected_version 和文件冲突相关处理，可作为条件保存适配线索。[S06] 必须实测其并发语义、可读写权限和版本标识范围，不能仅凭字段名称认定原子性。

缺少条件保存时，客户端可在上传前比较大小、修改时间或摘要，但这只是尽力检测；检查与写入之间仍有竞争窗口。对关键文件提供“仅下载到本地编辑”或要求增强通道。不能把客户端的二次 stat 描述为可靠的原子并发控制。

## 11.3 冲突界面

冲突面板同时显示打开时基线、当前远端内容和用户本地修改，支持重新加载、复制本地草稿和手动合并。强制覆盖默认不作为主按钮，且需要实际权限和二次确认。远端文件在比较时又发生变化，应重新进入冲突状态。

文件保存结果未知时，冻结自动重试，提供重新读取远端并比较。保存可能已经成功但确认丢失，不能再次覆盖他人的后续修改。目录、文件名或权限发生变化时，保留本地草稿并要求用户重新确认目标。

## 11.4 本地草稿与副作用

敏感内容默认只保存在内存。启用崩溃恢复时，使用加密草稿与系统安全存储管理密钥，设置保留期和注销策略；普通 SQLite 不直接保存文件正文。编辑器不自动打开外部语言服务或上传文件内容。

保存配置不自动执行 reload/restart，不自动调整权限或属主。需要执行命令时转到明确的终端上下文，由用户审阅并执行。客户端不为“好用”隐藏具有生产影响的步骤。

# 12 统一 Picker 与键盘工作流

## 12.1 Provider 设计

Picker 由 SearchProvider 提供结果，由 CommandRegistry 执行动作。搜索和执行分离：结果被选中时再次校验上下文和可用性，不能信任几秒前的缓存权限。

| 前缀建议 | 搜索范围 | 默认确认动作 |
| --- | --- | --- |
| 无前缀 / @ | 收藏、最近和已授权资产 | 有匹配连接则聚焦，否则展示账号选择并连接 |
| tab: | 已打开标签和分屏 | 聚焦目标视图，不创建重复连接 |
| / | 当前文件上下文、书签及已加载目录 | 跳转目录或打开文件，不自动执行命令 |
| # | 当前 MySQL 会话的库表元数据 | 打开表数据，显示完整数据库上下文 |
| > | 应用命令 | 调用同一命令注册表 |
| snippet: | 用户确认保存的命令/SQL 片段 | 插入编辑区域，不自动执行 |

上述前缀是本产品设计，不是 JumpServer 接口规范。结果至少包含名称、资源类型、站点、环境、资产/数据库、账号及操作提示，避免同名记录被误选。

## 12.2 搜索与排序

先返回本地收藏、最近访问和已有标签，再异步补充服务端授权资源。远程搜索采用初始 120–180 ms 防抖和明确的取消信号；输入新词时废弃旧请求 generation，旧响应不能覆盖新结果。

初始评分建议：文本匹配 60%、最近使用 15%、访问频率 10%、收藏 10%、当前上下文 5%。权重作为可调整启发式，不作为已验证最优算法。使用 Web Worker 执行较大规模本地匹配，避免阻塞终端输入。

搜索当前远程文件系统时，默认不递归遍历根目录。递归搜索必须由用户明确选择范围，并限制深度、数量、耗时和并发；服务端没有搜索 API 时，只搜索已加载索引，并标明范围。自动补全不能暗中执行 find/grep。

## 12.3 快捷键与焦点

| 动作 | Windows/Linux 建议 | macOS 建议 | 生效范围 |
| --- | --- | --- | --- |
| 命令面板 | Ctrl+Shift+P | Cmd+Shift+P | 全局，可配置 |
| 资源 Picker | Ctrl+Shift+K | Cmd+K | 全局，可配置 |
| 搜索当前终端 | Ctrl+Shift+F | Cmd+F | 终端视图 |
| 执行选中 SQL | Ctrl+Enter | Cmd+Enter | SQL 编辑器 |
| 预览并提交表格修改 | Ctrl+S | Cmd+S | 仅数据网格 |
| 保存远程文件 | Ctrl+S | Cmd+S | 仅文件编辑器 |

输入法组合输入期间，不把 Enter 当成选中 Picker 结果或执行 SQL。Esc 优先关闭当前浮层，再返回原焦点，不一路冒泡关闭整个连接。快捷键冲突检测和自定义映射属于设置功能。

## 12.4 命令注册表

命令包含 id、label、contextPredicate、canExecute、riskLevel 和 handler。canExecute 的客户端结果只用于界面，服务端仍进行最终授权。菜单、右键和 Picker 展示同一禁用原因，避免某个入口可以绕过另一个入口的限制。

命令执行时将站点、账号、资产和当前标签作为显式上下文传入；不得读取容易在异步期间变化的“全局当前资产”。结果返回前若用户已经切换上下文，只更新原对象，不跳转到另一个连接。

# 13 MySQL 查询工作台与数据表示

## 13.1 查询连接与控制台

MySQL 模块通过 ChenAdapter 处理会话、元数据和查询。每个 SQL 控制台绑定明确的数据库上下文；并发查询优先使用独立的受授权控制台。不能假设同一个 Chen 会话里的所有标签共用或不共用数据库连接，必须通过版本适配和测试确认。

执行链路包括授权检查、请求接收、执行、结果分批传输、终态确认。主会话心跳与查询取消不能被查询串行队列阻塞。关闭标签或浏览器 WebSocket 不等于确认 SQL 已停止。

## 13.2 元数据与 SQL 编辑

元数据树展示库、表、视图、字段、主键、可用索引与字段注释。只获取目标账号实际可见的对象，不用管理员连接为普通用户补全。缓存绑定身份和数据库，提供显式刷新；结构变化导致旧编辑计划失效。

Monaco 负责编辑体验；补全由本地元数据 Provider 提供。支持选中片段执行、整段执行、格式化、查询日志与收藏。不能用分号字符串切割实现任意 SQL 脚本解析，也不能通过正则判断“以 SELECT 开头”就认为无副作用。复杂脚本不在核心版可靠范围内时，明确要求用户选择单条语句执行。

生成的表浏览查询可以安全构造分页；用户任意 SQL 不得简单在末尾追加 LIMIT。执行计划、查询分析也应明确可能执行或消耗资源，不能把所有分析按钮当作无副作用操作。

## 13.3 分页、排序与结果缓存

表浏览默认每页 200 行，提供 50/200/500 的初始选项。采用稳定排序并附加主键作为相同值的 tie-breaker。大表连续浏览优先使用经过适配验证的 keyset 分页；任意跳页可采用 offset，但提示深分页代价，不把界面虚拟滚动当成数据库分页。

分页结果不是天然一致性快照，数据在页间变化时应允许刷新并说明范围；需要一致性导出时，必须使用经过支持的快照/事务策略并控制持续时间。结果集必须报告已取回行数、截断标志和下一批句柄。默认不自动 COUNT(*) 计算大表精确总量。导出默认当前结果或选中范围，导出全部时需明确发起独立任务并校验导出权限；查询权限不当然等于允许所有外传方式。

## 13.4 精确数据类型

| 数据类别 | 内部建议表示 | UI 与写入要求 |
| --- | --- | --- |
| NULL | 独立 null 标签 | 不等于空字符串，也不等于 JSON 字符串 "null" |
| 整数/BIGINT | 十进制字符串 + 整数类型 | 不先转为 JavaScript Number；复制保留精确值 |
| DECIMAL/NUMERIC | 十进制字符串 + scale 信息 | 不使用浮点数处理中间值；写入按目标精度验证 |
| FLOAT/DOUBLE | 明确浮点类型的文本表示 | 不宣称精确十进制；必要时只读保留原始表示 |
| DATE/DATETIME/TIMESTAMP | 类型 + 原始值 + 会话时区 | 不无条件套用本机时区；微秒精度需保留 |
| TIME | 原始时长文本 + 类型 | 不当作只有 00:00–23:59 的钟表时间 |
| JSON | 保留原始文本，独立编辑器 | 解析验证与格式化必须考虑大整数，避免精度丢失 |
| BLOB/BINARY/BIT | 字节或受限对象句柄 | 预览与下载分离；不能按 UTF-8 自动覆盖 |
| 默认值/生成列 | omitted/default 与只读标识 | 默认值不是字符串 "DEFAULT"；生成列默认禁改 |

MySQL 文档将 DECIMAL 归为精确数值类型；DATETIME 与 TIMESTAMP 的时区处理不同。因此这些语义需要贯穿服务端序列化、IPC、UI 和回写，不是在显示层补救。[S20][S21]

第一版可以对尚未实现的特殊类型、超大字段和几何类型只读，但必须标明具体原因。读取链路一旦已经丢失精度，客户端把 Number 再转成字符串无法恢复原始值，应将该类编辑能力禁用或修复服务端传输。

## 13.5 取消与连接清理

取消请求有独立 requestId 和确认状态，UI 显示“正在取消”，直到得到明确结果或进入未知状态。MySQL 的 KILL QUERY 和 KILL CONNECTION 语义不同，并受到权限约束；不能为了取消查询要求全局高权限。[S27]

即使语句被终止，整个事务也不一定已回滚。适配器需根据连接事务状态执行经过支持的回滚或关闭清理，并对非事务写入如实提示。连接归还前重置数据库、autocommit、会话变量与临时状态；无法确认状态干净的连接直接丢弃，不给其他标签复用。

# 14 MySQL 表格 CRUD 与事务语义

## 14.1 可编辑范围

核心版只对已确认来源的单个基础表开放行编辑。UPDATE/DELETE 要求可靠主键，联合主键必须完整支持；没有主键、来源不明确、JOIN、聚合和不可可靠定位的结果默认只读。第一版默认禁止修改主键本身。

新增可以在数据库语义上不依赖主键，但为了稳定刷新和行定位，核心版统一优先覆盖有主键表；其他情况列为显式受限模式。不得用“第 5 行”或当前排序位置作为数据库行标识。

Chen 固定源码已经存在 changes、insertRows、deleteRows，以及预览和保存入口；这证明有可参考的结构化路径，不证明所有部署或联合主键等细节都受支持。[S07][S08]

## 14.2 编辑状态与交互

```text
Clean -> Dirty -> Previewing -> PreviewReady -> Applying
          ^            |             |             |
          |          Invalid      修改后失效       +-> Committed
          |                                        +-> RolledBack
          +---------------- Conflict --------------+
                                                   +-> OutcomeUnknown
```

新增行、单元格修改和待删除行形成变更集；用户修改一个单元格不会立即执行 SQL。支持本地撤销/重做、批量粘贴预检和逐项移除变更。切页或刷新时必须提示未提交变更，不无声丢弃。

提交按钮显示新增、修改和删除的数量。预览展示目标站点/环境/账号/库表、列差异和预期影响行数。预览后任何编辑、结构变化或连接重建都使旧预览失效。展示的 SQL 是可读说明；服务端不能信任客户端拼出的执行 SQL。

## 14.3 安全写入规则

所有数据值通过受控执行层参数化；数据库名、表名和列名来自已验证元数据并正确引用。MySQL 参数标记不能替代 SQL 标识符，不能写成 UPDATE ? SET ? = ? 来解决表列名问题。[S17]

NULL、DEFAULT、遗漏字段和显式值分别建模，不允许任意表达式偷偷混入单元格值。新增时省略自增和生成列；修改只包含真实发生变化的字段。联合主键使用打开记录时的完整原始键值，不使用用户改写后的定位值。

命令 ACL 必须覆盖实际生成的每条语句及对应上下文；参数化不能导致原有审计只看见问号而失去关键语义。扩展时按现有服务端要求提供受控审计表示，保留参数类型，必要时脱敏，不把敏感参数打印到客户端诊断日志。

## 14.4 推荐的短事务算法

以下为拟议可靠写入算法；只有现有 Chen 经验证具备等价语义，或部署了获批扩展时才可使用：

1. 服务端验证会话、目标表、字段类型、权限、批次大小及预览版本；审批或用户确认在获取数据库锁之前完成。
2. 取得该批次独占连接，确认所有目标写入具有所需事务保障；开始短事务。
3. 按稳定主键顺序读取并锁定待修改/删除的记录，校验当前值与打开时的服务端快照或认可版本条件。
4. 发现记录消失、结构变化或冲突则停止批次并回滚；不要在持锁期间等待用户操作冲突对话框。
5. 参数化执行新增/修改/删除，检查匹配行与变更行语义、主键唯一性及数据库错误。
6. 完成审计所需记录安排后提交；确认提交成功才报告 committed；提交阶段通信失败报告 unknown。
7. 释放连接并重新读取受影响数据；更新本地基线，保留必要的结果与关联 ID。

MySQL 的锁定读可用于在事务内保护后续更新，但具体权限、隔离级别和锁范围必须在目标版本测试。[S19] 原子批次仅适用于所有相关副作用均可事务化的条件；非事务表以及触发器触及非事务对象的情况，不能宣称全量回滚。

## 14.5 冲突检测的精度

业务表已有并且所有写入方正确维护 version 字段时，可以使用版本条件；客户端不能擅自给业务表增加版本列，也不能假设别人会更新它。

无可靠版本列时，可在锁定当前行后由服务端对比原始快照。字符串比较需尊重既定的精确比较策略，不能只依赖大小写不敏感排序规则下的 SQL 等值判断；NULL、二进制、时间精度和 JSON 表示要有确定规范。没有历史版本标识时，无法识别数据曾变化又恢复的所有历史情况，应说明保证的是提交时的状态一致性。

若当前接口不能实现经过验证的冲突保护，产品不得把“按主键覆盖更新”描述为乐观锁。可提供明确的受限覆盖模式或要求扩展，但默认安全模式不静默覆盖。

## 14.6 影响行数、未知结果与回滚

affectedRows 不总能直接等同于“记录存在且发生一次修改”；驱动选项和相同值更新会影响含义。以经过测试的匹配/改变语义结合锁定结果判断，不能仅用 affectedRows = 0 断言记录已被删除。

提交结果未知时冻结该变更集自动重试，提示刷新核实。单纯添加 requestId 只能帮助关联，不能实现跨数据库提交崩溃窗口的 exactly-once。若操作记录与目标 DML 不能在同一原子边界持久化，仍需保留 unknown 状态。

已提交但刷新失败，显示“提交成功，刷新失败”；已提交但审计上报异常，必须分别报告数据结果和审计状态，并按服务端策略阻止后续操作，不能错误显示“数据库已回滚”。许多 DDL 会隐式提交，因此结构修改不混入这个表格事务流程。[S18]

# 15 内部接口与数据契约

## 15.1 设计约束

本章类型为项目内部契约，不是可直接发送给 JumpServer 的 JSON。适配器负责映射已验证的官方协议；不能映射的字段或语义要返回 unsupported，禁止丢掉联合主键、类型或并发条件后继续执行。

```typescript
type DbValue =
  | { kind: 'null' }
  | { kind: 'text'; value: string }
  | { kind: 'integer'; decimal: string }
  | { kind: 'decimal'; decimal: string; scale?: number }
  | { kind: 'float'; literal: string }
  | { kind: 'temporal'; sqlType: string; raw: string;
      sessionTimeZone?: string }
  | { kind: 'json'; raw: string }
  | { kind: 'binary'; objectRef: string; byteLength: string };

type WriteValue = DbValue | { kind: 'default' };
interface TableRef { schema: string; table: string; }
interface RowIdentity {
  key: Array<{ column: string; value: DbValue }>;
  serverRowRef?: string;
  snapshotRef?: string;
}
type Change =
  | { kind: 'insert'; changeId: string;
      values: Record<string, WriteValue> }
  | { kind: 'update'; changeId: string; row: RowIdentity;
      values: Record<string, WriteValue> }
  | { kind: 'delete'; changeId: string; row: RowIdentity };
interface ChangeSet {
  table: TableRef;
  schemaRevision: string;
  changes: Change[];
}
```

snapshotRef 和 serverRowRef 只有由可信服务端生成并验证时才具有保护意义。客户端自建摘要不能替代服务端授权。二进制 objectRef 是受限对象句柄，不是允许读取任意路径的字符串。

## 15.2 服务接口草案

```typescript
interface TerminalService {
  open(context: ResourceContext): Promise<SessionRef>;
  send(session: SessionRef, data: Uint8Array): Promise<void>;
  resize(session: SessionRef, cols: number, rows: number): Promise<void>;
  close(session: SessionRef): Promise<void>;
}
interface FileService {
  list(session: SessionRef, path: string): Promise<FilePage>;
  startUpload(request: UploadRequest): Promise<TaskRef>;
  cancelTask(task: TaskRef): Promise<CancelResult>;
  save(request: ConditionalSaveRequest): Promise<SaveResult>;
}
interface DatabaseService {
  execute(request: QueryRequest): Promise<QueryRef>;
  cancel(query: QueryRef): Promise<CancelResult>;
  preview(session: SessionRef, changes: ChangeSet): Promise<Preview>;
  apply(session: SessionRef, preview: PreviewRef): Promise<ApplyResult>;
}
```

FilePage、UploadRequest、Preview 等为后续模块需实现的领域类型占位符；本段是接口轮廓，不是完整可编译 SDK。每个正式类型均需有字段说明、范围约束、错误码和单元测试。输入中的会话身份只能与服务端已认证对象比对，不能直接作为授权来源。

## 15.3 结果对象

```typescript
interface ApplyResult {
  operationId: string;
  outcome: 'committed' | 'rolled-back' | 'not-started' | 'unknown';
  refresh: 'completed' | 'required' | 'not-applicable';
  audit: 'confirmed' | 'pending' | 'failed' | 'unknown';
  perChange?: Array<{ changeId: string; status: string }>;
  error?: AppError;
}
```

结果状态彼此独立，UI 不把“刷新成功”当作“提交成功”的唯一判断。原协议无法确认审计落地状态时保留 unknown，不自行显示 confirmed。

## 15.4 统一错误模型

| 错误类别 | 代表内部码 | 界面动作 |
| --- | --- | --- |
| 认证/授权 | AUTH_EXPIRED、PERMISSION_DENIED | 重新登录或请求权限；不自动换管理员身份 |
| 协议/能力 | PROFILE_UNSUPPORTED、CAPABILITY_UNKNOWN | 展示部署信息及受限能力 |
| 网络 | CONNECT_TIMEOUT、SESSION_LOST | 可重连；禁止重放未知写入 |
| 文件 | FILE_CONFLICT、LOCAL_PATH_DENIED | 差异处理、重新选择目标 |
| 数据编辑 | ROW_CONFLICT、SCHEMA_CHANGED | 刷新与重新预览，不覆盖原草稿 |
| 结果不确定 | WRITE_OUTCOME_UNKNOWN | 停止自动重试，指导核实 |
| 数据已变更 | COMMITTED_REFRESH_FAILED | 明确提交成功，单独重试只读刷新 |

错误消息供用户理解，correlationId 用于诊断；不把原始堆栈、令牌或 SQL 参数直接显示为通知。前端文案不抹去服务端真实拒绝原因。

# 16 可选服务端增强设计

## 16.1 何时需要增强

当目标部署缺少核心安全语义，而客户端无法补足时，选择升级到经验证的配套版本，或部署经过评审的 KoKo/Chen 小范围扩展。增强必须沿用现有用户会话、目标账号、授权控制、连接终止和审计；不另建一个持有数据库密码的旁路服务。

优先改动组件边界，不直接修改业务数据库结构。不将开发分支整套代码未经测试替换生产组件；增强版本须与 Core、依赖库和网关配置联测，并可回滚。

## 16.2 文件传输增强契约

建议具备 prepare、write/read、status、commit、cancel 五类语义，而不是仅暴露任意偏移写文件。服务端生成任务 ID，绑定用户、组织、资产、账号、规范化路径、目标版本和过期时间。

write 以服务端确认的偏移或块序号去重；status 返回已持久化进度；commit 校验内容和目标冲突再完成提交。cancel 区分停止接收与删除临时文件，并提供可核对的清理结果。临时文件容量、并发、保留期和权限都有配额。

这些是拟议语义，不是承诺当前 KoKo 同名动作已完整实现。断线后能否恢复、恢复所需身份和目录根约束都由服务端明确执行。同步文件摘要不能替代业务授权，也不能防止所有目标路径竞争。

## 16.3 数据编辑增强契约

建议提供 typed preview、typed apply 和 operation status。预览返回服务端生成的计划引用、作用域、结构版本、风险提示和有效期；应用时重新校验，防止客户端修改预览内容或挪用其他会话的计划。

apply 在组件内独占连接执行短事务，并在同一会话范围处理 ACL、取消与审计。operation status 只返回该用户可见操作。状态存储未与目标数据库事务形成原子提交关系时，必须保留 unknown，不能承诺自动重试绝不重复。

## 16.4 增强的验收门槛

必须验证越权、路径穿越、符号链接逃逸、计划篡改、跨会话重放、断线和重启、审计异常、取消竞态与资源回收。增强接口只通过受信网关访问，不向桌面端分发组件注册密钥，也不增加可直连数据库的公共监听端口。

不允许修改服务端且能力不足时，客户端显示受限模式并给出明确兼容报告。这是部署条件限制，不通过绕过授权或伪造能力解决。

# 17 本地存储、安全与可观测性

## 17.1 数据分级与持久化

| 数据 | 默认保存位置 | 保留与清理规则 |
| --- | --- | --- |
| 站点配置、主题、布局 | SQLite | 用户可导出；不含凭据；版本化迁移 |
| 收藏、最近访问、元数据缓存 | 身份作用域内 SQLite | 可清理；授权刷新后移除失效引用 |
| 登录凭据、短期令牌 | 内存；必要时系统安全存储 | 按服务端寿命与用户设置处理；注销清理 |
| SQL 草稿、远程文件草稿 | 默认内存，可选加密草稿 | 显式启用恢复；设置保留期和清理策略 |
| 查询结果、终端输出 | 默认内存有界缓存 | 关闭/注销按策略清理，不默认持久化 |
| 传输文件 | 用户授权的目标路径、受控临时目录 | 完成、失败和注销均有临时文件清理流程 |
| 诊断日志 | 脱敏滚动日志 | 限容量和保留期，不记录业务内容 |

SQLite 文件本身不因“本地数据库”而自动安全。敏感草稿需要独立加密方案和密钥保护；系统安全存储不可用时，禁用敏感恢复而不是明文降级。[S15]

## 17.2 本地表结构建议

建议建立 sites、identity_scopes、workspaces、tabs、favorites、recent_resources、capability_profiles、transfer_journal、draft_manifest 和 settings 表。每条用户相关记录包含 scopeId；唯一键不只使用 assetId。布局引用和草稿内容分开存储，SQLite 中仅保存受控内容句柄。

所有迁移有版本、事务和备份策略。应用崩溃后将运行中的任务恢复为 interrupted/unknown，先向服务端查询或由用户确认，不从本地 journal 自动推断写入失败后重做。降级到旧客户端前检查 schema 兼容性，禁止自动读取未知格式并覆盖。

## 17.3 威胁模型与控制

| 威胁 | 重点控制 |
| --- | --- |
| 恶意终端输出、字段文本、文件名 | 只作数据展示；限制控制序列和外链；不执行 HTML |
| Renderer 被利用 | contextIsolation、sandbox、禁用 nodeIntegration、最小 preload |
| IPC 任意文件读写 | 主进程授权句柄、目标绑定、输入校验和路径限制 |
| 凭据串站或重定向泄漏 | 身份隔离、域名白名单、每次重定向复核 |
| 文件路径逃逸 | 双端规范化、根目录约束、符号链接和压缩包检查 |
| 数据库批次篡改 | 可信元数据、服务端计划引用、参数化和重新授权 |
| 更新被替换 | 签名、来源校验、受控发布与回滚验证 |
| 本地敏感信息残留 | 最小持久化、加密草稿、诊断脱敏和清理 |

Electron 安全配置按官方指南实施。生产构建使用严格 CSP，只加载随包交付的核心脚本和编辑器资源；不依赖外部 CDN 注入代码，不启用远程页面的高权限桥接。[S11]

## 17.4 TLS、网络和系统集成

默认仅允许 HTTPS/WSS；企业私有 CA 通过受控信任配置解决，不提供全局忽略证书错误选项。验证站点证书变化和重定向，支持目标部署需要的代理配置；HTTP 与 WebSocket 代理路径都要测试，不能只验证 API 请求。

自定义协议 deep link 第一版只允许打开已配置站点/资源的引用，不在 URL 中携带凭据或自动执行命令。所有可触发新连接的外部输入都需要用户确认和当前授权校验。

## 17.5 审计与权限的真实边界

客户端日志不是 JumpServer 服务端审计的替代品。验收必须用实际普通用户检查终端、SFTP、查询和 CRUD 的记录归属，并验证拒绝命令、文件限制和管理员终止。

授权撤销对既有会话何时生效取决于目标部署的服务端语义；不能把“已删除资产授权”自动等同于“存量连接已立即断开”。如产品要求即时生效，需要服务端提供相应保证并实测，不满足则写入兼容报告并限制发布承诺。

对已经展示在用户屏幕上的数据，客户端不能保证阻止截图或所有复制行为。导出限制应准确表述其覆盖的操作范围，不宣称实现了完整数据防泄漏。

## 17.6 可观测性

为登录、连接、文件任务和数据库操作生成不含秘密的关联 ID；记录阶段耗时、重试次数、缓冲高水位、失败类别和终态。默认遥测关闭，启用时需明确组织策略和数据范围。

诊断包应在导出前提供预览和脱敏检查。服务端 SQL 审计与客户端性能日志采用不同内容策略；不得为了复现错误收集用户输入的密码、连接令牌或全量数据库记录。

# 18 测试策略与质量门槛

## 18.1 测试层次

**单元测试：** 路径规范化、命令上下文、精确类型、变更集归并、状态机转换、能力降级和错误分类。

**协议契约测试：** 固定版本的脱敏握手/消息样本、二进制分帧、未知消息、乱序响应、重复事件、会话代际和关闭流程。Replay 测试只验证客户端协议处理，不能代替真实授权与审计验证。

**端到端测试：** 使用隔离的 JumpServer、SSH 和 MySQL 测试资产，从普通账号登录完成四条核心工作流。目标 KoKo/Chen 必须与发布兼容清单一致。

**故障与安全测试：** 在鉴权、写入、提交、确认和刷新阶段注入断网、重启、权限变化、取消以及恶意输入，检查是否误报成功或重复执行。

## 18.2 必测矩阵

| 测试 ID | 关联需求 | 场景与通过标准 |
| --- | --- | --- |
| T-A01 | R-A01 | 登录、MFA/重定向成功与失败；不跳过现有认证 |
| T-A02 | R-A01/R-S01 | 两站点两用户切换；Cookie、Picker、草稿和会话不串用 |
| T-A03 | R-A02 | 授权账号选择正确；不展示或连接无权限资产 |
| T-W01 | R-W01 | 标签切换、分屏、缩放不重连且焦点正确 |
| T-W02 | R-W01 | 崩溃恢复布局和草稿，不自动执行或恢复未知写入 |
| T-T01 | R-T01 | 中文输入法、emoji、组合键、粘贴和高 DPI |
| T-T02 | R-T01 | vim/top/tmux、窗口 resize、鼠标模式正确 |
| T-T03 | R-T01 | 持续大输出期间 Picker 和输入仍可用，缓冲受控 |
| T-F01 | R-F01 | 上传下载、零字节、目录、隐藏文件和同名冲突 |
| T-F02 | R-F02 | GB 级传输，关闭面板继续、注销停止，内存有界 |
| T-F03 | R-F02 | 中断/重试/续传符合能力；不伪造摘要验证 |
| T-F04 | R-F01/R-S01 | 路径穿越、链接环路、本地保留文件名、压缩包逃逸 |
| T-E01 | R-F03 | 两客户端修改同一文件；冲突不被静默覆盖 |
| T-E02 | R-F03 | 保存成功后确认丢失；进入未知状态而非自动覆盖重试 |
| T-P01 | R-P01 | 大候选集搜索、键盘选择、旧请求晚到、焦点恢复 |
| T-P02 | R-P01/R-S01 | 同名资产/表和授权变化；执行时重新校验上下文 |
| T-D01 | R-D01 | 查询、分页、排序、取消、多结果和错误展示 |
| T-D02 | R-D02 | BIGINT/DECIMAL/NULL/JSON/微秒时间往返不丢失 |
| T-D03 | R-D02 | 单主键与联合主键 CRUD；无主键/复杂结果明确只读 |
| T-D04 | R-D02 | 两客户端编辑相同行；冲突处理符合声明的保证 |
| T-D05 | R-D02 | 唯一键、外键、CHECK、只读列错误不导致误提交 |
| T-D06 | R-D02 | 批次中途失败；事务模式确实回滚所有相关写入 |
| T-D07 | R-D02 | COMMIT 前后断网；准确区分未开始/回滚/提交/未知 |
| T-D08 | R-D02 | 已提交但刷新失败；显示成功并仅重试只读刷新 |
| T-S01 | R-S01 | 每类操作的服务端审计可定位到真实用户和目标账号 |
| T-S02 | R-S01 | 管理员终止、令牌过期、权限收回的实际行为被记录 |
| T-S03 | R-S01 | 远端文本不能触发任意脚本、IPC、外链或剪贴板写入 |
| T-U01 | 全部 | 安装、升级、配置迁移和回滚不破坏工作空间及隔离 |

## 18.3 性能验收目标

以下全部是待实测的初始目标，不是已有实现的成绩。每份报告记录操作系统、CPU、内存、显示分辨率、应用/服务端版本、网络 RTT、数据集和采样方法。界面响应与网络/数据库耗时分开统计。

| 场景 | 初始验收目标 |
| --- | --- |
| 本地 Picker | 10,000 条已缓存候选，结果更新 P95 不高于 100 ms |
| 已打开标签切换 | 不重连；交互可用 P95 不高于 100 ms |
| 终端 | 10 个会话、4 个可见分屏下无明显输入阻塞；输出缓冲有上限 |
| 文件传输 | 4 个并发任务，传输额外缓冲预算初值不超过 128 MiB，基础运行时内存另计 |
| 文件列表 | 10,000 条已加载项虚拟化滚动；不得误称为服务端分页能力 |
| 数据网格 | 200 行分页和宽表下连续键盘编辑流畅；结果缓存按预算淘汰 |
| 混合负载 | 上传大文件同时输入终端和使用 Picker，不出现长时间主线程卡死 |

建议在约 50 ms RTT、无丢包的基准网络以及高延迟/1% 丢包的故障网络分别测试。网格虚拟化和终端渲染的平滑度用帧耗时与长任务衡量，不只凭肉眼或平均 FPS。

## 18.4 发布阻塞条件

存在凭据泄漏、跨用户数据串用、绕过授权、未知写入自动重放、错误地报告回滚、精确数值损坏、文件无提示覆盖或关键审计缺失时，不发布正式核心版。尚未通过服务端能力测试的增强功能必须关闭，不能只在帮助文档中免责。

# 19 工程组织、里程碑与发布

## 19.1 仓库建议

采用单仓库管理桌面运行时、UI、领域模型、适配器和测试数据。按领域边界组织，而不是让页面组件直接依赖散落的 HTTP/WebSocket 代码。UI 可通过模拟 adapter 独立开发，真实 adapter 使用同一接口。

每次变更需要配套测试。协议适配变更必须同时更新兼容 profile、消息样本和能力说明；涉及数据库写入必须评审提交语义和错误结果，不能仅检查截图。

## 19.2 工作包与依赖

| 工作包 | 主要交付 | 前置依赖 | 完成门槛 |
| --- | --- | --- | --- |
| W0 接入基线 | 部署清单、认证方案、能力报告 | 测试环境与普通账号 | 明确核心版能否在当前服务端成立 |
| W1 设计系统 | 工作空间、视觉令牌、焦点规范、Picker 原型 | 需求确认 | 四条任务走查通过 |
| W2 桌面安全骨架 | 隔离登录、IPC、配置、诊断 | W0 | 无通用高权限桥接 |
| W3 终端适配 | 协议、实例管理、resize、输入和流控 | W2 | T-T 系列通过 |
| W4 文件能力 | 管理器、传输队列、编辑器、冲突处理 | W2/W3 上下文 | T-F/T-E 系列通过 |
| W5 Picker | Provider、缓存、命令、焦点 | W1/W2 | 搜索性能与作用域正确 |
| W6 MySQL 查询 | Chen 会话、元数据、执行和取消 | W0/W2 | T-D01 通过 |
| W7 MySQL CRUD | 类型模型、变更集、预览、事务结果 | W6；必要增强 | T-D02 至 T-D08 通过 |
| W8 联测发布 | 混合负载、安全、签名、迁移和回滚 | W3 至 W7 | 全部发布阻塞项清零 |

团队至少需要明确产品/交互、前端工作台、桌面/协议、测试与服务端支持五类责任；人员可以兼任，但认证、传输和数据库事务需要指定审查人。未明确具体版本和服务端缺口前，不给出貌似精确的交付工期。

## 19.3 里程碑门禁

**G0 可行性门禁：** 一条 SSH、一条文件链路、一条 MySQL 查询和写入链路均跑通，并核对授权与审计。服务端缺口有明确处置方案。

**G1 体验门禁：** 使用模拟或测试数据完成工作空间、Picker、文件与数据库工作流；键盘与错误状态评审通过。

**G2 核心版门禁：** 全部核心需求有真实端到端验证，不能以外部工具跳转代替文件管理或 CRUD。

**G3 稳定性门禁：** 故障注入、混合负载、安装升级与安全测试通过；增强功能按兼容能力启用。

## 19.4 打包、签名与更新

使用锁定依赖和可复现的构建流程，发布包附带校验信息、依赖清单和兼容矩阵。Windows/macOS 签名与相应平台验证按官方流程实施；签名和更新机制的能力可参考 Electron 文档。[S28][S30]

默认支持企业受控更新源和离线安装，不强制联网。更新前保存工作布局，检测进行中的文件任务和数据库操作；不能在用户写入中途静默重启。更新失败回退客户端版本时，还要检查本地数据 schema 与服务端 adapter 是否可用。

## 19.5 发布文档

每个版本至少包含支持平台、已验证 JumpServer 组件组合、已知限制、数据处理说明、安全默认值、升级/回退方法和诊断方式。未知能力应在应用“连接诊断”页显示，而不是仅藏在 release notes 中。

# 20 风险、待确认项与最终验收

## 20.1 风险登记

| 风险 | 后果 | 控制措施 |
| --- | --- | --- |
| 把开发提交当稳定版本 | 连接或写入协议不兼容 | 固定部署 profile；代码证据与发行能力分开 |
| 认证与 WS 会话不一致 | API 成功但终端/数据库无法连接 | 优先打通 AuthBroker、Cookie、Origin 和票据链路 |
| 服务端缺少事务/冲突能力 | GUI 写入不可靠 | 核心发布门禁；获批增强或兼容版本 |
| 大文件压垮 UI | 输入延迟、进程崩溃 | 独立任务执行、分块、有界队列和压测 |
| 数据精度或行定位错误 | 误改记录、金额或主键损坏 | 精确类型与完整主键、无能力只读 |
| 错误重试未知写入 | 重复插入或覆盖后续修改 | outcome 建模；无可靠去重不自动重试 |
| 目录联动错误 | 在错误目录或权限上下文操作 | 可选 shell integration、路径映射校验 |
| 升级破坏兼容 | 新客户端无法使用旧服务端 | 兼容清单、回归样本、配置迁移与回滚 |

## 20.2 项目启动时补齐的信息

| 待确认项 | 记录要求 | 不阻塞本文成立的默认处理 |
| --- | --- | --- |
| JumpServer/KoKo/Chen 版本 | 版本、镜像 tag/digest、定制补丁 | 暂不宣称任何具体稳定版完全兼容 |
| 服务端变更权限 | 可升级、可加补丁、完全不可改 | 按不改服务端基线推进，增强单独审批 |
| 认证方式 | 实际启用方式、MFA、重定向和过期策略 | 不预设企业 SSO 可用 |
| 网络拓扑 | 网关子路径、代理、CA、组件入口 | 所有 URL 从受控站点配置解析 |
| 首发平台 | 用户设备与系统版本分布 | 三平台设计，按真实优先级安排联测 |
| MySQL 范围 | 版本、字符集、引擎、主键和特殊类型 | 先验证有主键的事务表；特殊类型明确只读 |
| 文件策略 | 根目录、大小、上传下载/编辑权限 | 不默认允许任意路径或覆盖 |
| 安全策略 | 草稿、导出、日志、审计与终止要求 | 最小持久化，不默认外传业务数据 |

这些是开发启动清单，不要求用户再次回答已明确的社区版与自研偏好。收集信息后更新兼容矩阵和版本化设计决策，不把未确认项留在代码里作为隐含假设。

## 20.3 最终验收场景

在普通测试用户下，从资产 Picker 打开 SSH，分屏操作，打开同一资产文件面板，完成上传、下载、重命名及文本编辑；随后搜索 MySQL 表，完成查询、新增、修改、删除和变更预览。另一个测试用户无法看到其缓存、草稿和连接。

在这条流程中插入一次文件保存冲突、一次 SQL 批次错误、一次提交阶段断网以及一次管理员终止。产品应仍能明确说明发生了什么、哪些数据已改变、哪些结果未知，以及用户下一步可以安全执行什么。所有受要求的服务端审计可定位到真实执行者。

**交付的判断标准不是“页面齐全”，而是这四条核心任务既顺手，又不会在失败时误导用户。**

# 附录 A 代码目录与启动清单

## A.1 建议目录结构

```text
apps/desktop/
  main/                 # 窗口、身份代理、系统权限、更新
  preload/              # 窄桥接接口，不暴露原始 ipcRenderer
  utility/              # 会话、连接、任务执行
  renderer/             # 页面组合和应用入口
packages/
  domain/               # 领域类型、错误、能力和状态机
  workspace/            # 标签、分屏、焦点、布局
  command-center/       # 命令注册、Picker、Provider
  terminal/             # xterm 生命周期、输入策略
  files/                # 文件模型、编辑、传输状态
  database/             # 元数据、查询、精确值、变更集
  adapters-jumpserver/
    core/               # 认证与授权资源 API
    koko/               # 终端与文件协议
    chen/               # 数据库会话与消息
    profiles/           # 经验证的版本兼容信息
  desktop-contract/     # IPC schema 和运行时验证
  design-system/        # 设计令牌与共享控件
tests/
  unit/ contract/ e2e/ fault/ security/
  fixtures/             # 脱敏消息、测试文件和数据库数据
  compatibility/        # 每个部署 profile 的测试报告
docs/
  decisions/ protocols/ releases/ threat-model/
```

## A.2 前十项开发任务

| 顺序 | 任务 | 输出 |
| --- | --- | --- |
| 1 | 建立隔离测试环境并记录组件版本 | deployment-profile 与权限清单 |
| 2 | 手工走通官方连接流程并脱敏记录协议 | 握手与消息 fixtures，不包含令牌 |
| 3 | 定义领域类型、错误和会话代际 | domain 包及状态机测试 |
| 4 | 建立安全桌面壳和隔离登录窗口 | AuthBroker 与最小 IPC |
| 5 | 实现无样式但正确的 KoKo 终端 | 输入、输出、resize、关闭贯通 |
| 6 | 实现文件 list/upload/download | 流式任务与基本权限验证 |
| 7 | 实现 Chen 查询并检查精确类型 | 会话、元数据、结果、取消 |
| 8 | 验证表格写入语义与服务端缺口 | CRUD capability report |
| 9 | 开发工作空间与 Picker，再连接真实 adapter | 四条核心任务交互原型 |
| 10 | 加入故障注入及审计核对 | 作为合并和发布门禁的回归集 |

## A.3 每个协议 profile 的记录模板

```json
{
  "profileId": "jms-validated-profile-001",
  "status": "not-yet-validated",
  "deployment": {
    "coreVersion": "to-be-recorded",
    "kokoVersion": "to-be-recorded",
    "chenVersion": "to-be-recorded",
    "gatewayBasePath": "/"
  },
  "authFlow": "to-be-validated",
  "capabilities": {},
  "fixtures": [],
  "verifiedTestIds": [],
  "knownLimitations": []
}
```

该模板故意保留未验证状态，不能随客户端发布时自动当作 supported。版本与镜像摘要由管理员或可信部署信息填充，不采集秘密配置。

# 附录 B 测试数据与故障脚本要求

## B.1 MySQL 测试数据集

在明确隔离的测试数据库中建立：单整数主键表、联合主键表、无主键表、带唯一约束表、外键关联表、生成列表、含 BIGINT/DECIMAL/JSON/NULL/微秒时间/BLOB 的类型表，以及无法保证全量回滚的受限测试对象。不得对生产库执行测试建表或破坏性操作。

数据至少覆盖：超出 JavaScript 安全整数范围的整数、高精度小数、空字符串和 NULL、包含引号/反斜杠/换行的文本、中文与 emoji、相同排序值、极长文本、零字节二进制、JSON 内大整数和重复更新为相同值。

两个普通测试用户分别使用只读与可写目标账号。冲突测试由两个独立会话同时操作，不仅通过前端伪造错误消息。事务测试逐项核实数据库实际状态，而非仅观察 GUI 提示。

## B.2 文件数据集

准备零字节、小文本、超出编辑上限文本、GB 级文件、深目录、多文件目录、隐藏文件、含空格/引号/Unicode 的名称、符号链接与环路、只读路径和同名目标。跨平台下载测试还需覆盖大小写冲突和本地保留名称。

目录遍历和恶意路径测试在受控目录中进行，验证客户端和服务端都拒绝逃逸，不把攻击字符串转成真实越界写入。任务取消后核对远端临时文件和本地临时文件的清理结果。

## B.3 故障注入点

| 阶段 | 注入故障 | 必须核实 |
| --- | --- | --- |
| 登录后、握手前 | 令牌过期或会话清理 | 不死循环重试，不进入错误用户作用域 |
| 文件写入中 | 断网、磁盘满、权限撤销 | 已完成部分准确报告，任务不误报完成 |
| 文件提交后、确认前 | 丢弃完成响应 | 进入结果未知；不盲目覆盖重试 |
| SQL 执行前 | ACL 拒绝、结构变化 | 数据不改变，保留本地变更集 |
| SQL 批次中 | 约束错误、取消或连接故障 | 事务承诺与数据库实际结果一致 |
| COMMIT 发送前后 | 连接断开或组件重启 | 准确处理未知结果，不重复执行 |
| 数据提交后 | 刷新失败、审计异常 | 分开报告数据、刷新与审计状态 |
| UI 或 utility 崩溃 | 杀进程并重启 | 恢复布局/草稿，绝不自动重放写入 |

## B.4 验收报告模板

每项测试记录测试 ID、应用版本、服务端 profile、用户与账号角色、测试步骤、预期结果、实际结果、数据库/文件系统核对结果、审计关联 ID、脱敏截图或日志，以及未解决限制。性能数据额外记录机器和网络条件。

本文件提供的是测试设计，未执行这些测试。不能把待执行清单或模拟 adapter 的演示结果写成生产兼容证明。


# 附录 C 证据来源与版本边界

## C.1 核对范围

资料核对日期：2026 年 9 月 12 日。来源以 JumpServer、Electron、Tauri、xterm.js、MySQL、W3C 及对应项目官方资料为主。涉及服务端新能力的描述来自固定源码提交，不推断其已经进入用户部署或某个稳定发行版。

| 仓库 | 本文引用的固定提交 | 用途 |
| --- | --- | --- |
| jumpserver/luna | bb02a9846a8a3f21ce6bc84c9187fdb961b20cfb | 授权资产与连接令牌 API 参考 |
| jumpserver/koko | cb06f265992dd3ea4173432c028d50cfbf96b2f1 | 终端/文件路由和文件能力线索 |
| jumpserver/chen | d9890760dfdf87595bcaf6ca388fd96892f8f12f | 数据会话、WebSocket 及表格编辑线索 |

证据只能说明被引用资料中的事实；无法据此确认当前环境是否启用了相关组件、实际部署了何种代码、是否保留相同安全策略。本文没有实际运行协议、数据库、性能或故障注入测试。

## C.2 来源索引

**[S01] JumpServer V4 文档：Web 终端与数据库连接方式**

用于 MySQL Web GUI 及数据库原生客户端/X-Pack 边界；文档不代替部署能力验证。 [查看原始资料](https://docs.jumpserver.org/zh/v4/manual/admin/workbench/my_assets/web_terminal/)

**[S02] JumpServer 官方文档：系统架构与组件说明**

用于 Core、KoKo、Chen、Magnus 的职责区分。 [查看原始资料](https://docs.jumpserver.org/zh/latest/architecture/)

**[S03] JumpServer 官方文档：API 访问与认证**

用于部署内 /api/docs/ 和认证方式线索；参数以实际实例为准。 [查看原始资料](https://docs.jumpserver.org/zh/latest/dev/rest_api/)

**[S04] Luna：ui/composables/useApiRequest.ts**

固定提交 bb02a9846a8a；授权资产与 connection-token 接口参考。 [查看原始资料](https://github.com/jumpserver/luna/blob/bb02a9846a8a3f21ce6bc84c9187fdb961b20cfb/ui/composables/useApiRequest.ts)

**[S05] KoKo：pkg/httpd/webrouter.go**

固定提交 cb06f265992d；终端、SFTP、票据及兼容路由参考。 [查看原始资料](https://github.com/jumpserver/koko/blob/cb06f265992dd3ea4173432c028d50cfbf96b2f1/pkg/httpd/webrouter.go)

**[S06] KoKo：pkg/httpd/websftp.go**

固定提交 cb06f265992d；文件动作、条件保存字段与传输动作线索，非完整语义保证。 [查看原始资料](https://github.com/jumpserver/koko/blob/cb06f265992dd3ea4173432c028d50cfbf96b2f1/pkg/httpd/websftp.go)

**[S07] Chen：SaveChangesRequest.java**

固定提交 d9890760dfdf；修改、新增、删除请求结构。 [查看原始资料](https://github.com/jumpserver/chen/blob/d9890760dfdf87595bcaf6ca388fd96892f8f12f/backend/framework/src/main/java/org/jumpserver/chen/framework/console/entity/request/SaveChangesRequest.java)

**[S08] Chen：DataViewConsole.java**

固定提交 d9890760dfdf；数据浏览、ACL 和结构化预览/保存入口。 [查看原始资料](https://github.com/jumpserver/chen/blob/d9890760dfdf87595bcaf6ca388fd96892f8f12f/backend/framework/src/main/java/org/jumpserver/chen/framework/console/DataViewConsole.java)

**[S09] Chen：WebSocketConfig.java**

固定提交 d9890760dfdf；WebSocket 路由、Origin、子协议和 HTTP session 绑定。 [查看原始资料](https://github.com/jumpserver/chen/blob/d9890760dfdf87595bcaf6ca388fd96892f8f12f/backend/web/src/main/java/org/jumpserver/chen/web/config/WebSocketConfig.java)

**[S10] Electron 官方文档：Process Model**

用于 Main、Renderer、utility process 和进程通信设计依据。 [查看原始资料](https://www.electronjs.org/docs/latest/tutorial/process-model)

**[S11] Electron 官方文档：Security**

用于隔离、沙箱、导航、IPC 和远程内容安全边界。 [查看原始资料](https://www.electronjs.org/docs/latest/tutorial/security)

**[S12] Tauri 官方文档：Webview Versions**

用于系统 WebView 的跨平台差异与选型。 [查看原始资料](https://v2.tauri.app/reference/webview-versions/)

**[S13] xterm.js 官方指南：Flowcontrol**

用于终端写入缓冲、完成回调及流量控制。 [查看原始资料](https://xtermjs.org/docs/guides/flowcontrol/)

**[S14] xterm.js 官方指南：Security**

用于不可信终端输出和集成安全。 [查看原始资料](https://xtermjs.org/docs/guides/security/)

**[S15] Electron 官方 API：safeStorage**

用于系统安全存储与 Linux basic_text 降级风险。 [查看原始资料](https://www.electronjs.org/docs/latest/api/safe-storage)

**[S16] Electron 官方 API：net**

用于 Chromium 网络栈与网络会话接入；Cookie 和 WebSocket 集成仍需实测。 [查看原始资料](https://www.electronjs.org/docs/latest/api/net)

**[S17] MySQL 8.4 Reference Manual：PREPARE Statement**

用于参数标记和 SQL 标识符的边界。 [查看原始资料](https://dev.mysql.com/doc/refman/8.4/en/prepare.html)

**[S18] MySQL 8.4：Statements That Cause an Implicit Commit**

用于 DDL 与事务回滚承诺的限制。 [查看原始资料](https://dev.mysql.com/doc/refman/8.4/en/implicit-commit.html)

**[S19] MySQL 8.4：InnoDB Locking Reads**

用于短事务中的锁定读设计依据，实际权限和锁范围按版本测试。 [查看原始资料](https://dev.mysql.com/doc/refman/8.4/en/innodb-locking-reads.html)

**[S20] MySQL 8.4：DATE、DATETIME 与 TIMESTAMP**

用于日期时间类型、时区与精度处理。 [查看原始资料](https://dev.mysql.com/doc/refman/8.4/en/datetime.html)

**[S21] MySQL 8.4：Fixed-Point Types / DECIMAL**

用于精确十进制数值的表示与回写。 [查看原始资料](https://dev.mysql.com/doc/refman/8.4/en/fixed-point-types.html)

**[S22] Tabby 官方仓库**

用于 Terminus/Tabby 名称和终端交互参考；不复制品牌。 [查看原始资料](https://github.com/Eugeny/tabby)

**[S23] Termius 官方网站**

用于区分 Termius 与 Tabby，并作为资源/文件工作流参考。 [查看原始资料](https://www.termius.com/)

**[S24] Microsoft Monaco Editor 官方仓库**

用于编辑器能力与完整 IDE 的边界。 [查看原始资料](https://github.com/microsoft/monaco-editor)

**[S25] Glide Data Grid 官方仓库**

用于网格编辑、渲染及数据源职责边界。 [查看原始资料](https://github.com/glideapps/glide-data-grid)

**[S26] VS Code 官方文档：Terminal Shell Integration**

用于可选目录上报设计参考；不代表 KoKo 必然透传或支持。 [查看原始资料](https://code.visualstudio.com/docs/terminal/shell-integration)

**[S27] MySQL 8.4：KILL Statement**

用于终止查询/连接的差异和权限约束。 [查看原始资料](https://dev.mysql.com/doc/refman/8.4/en/kill.html)

**[S28] Electron 官方教程：Code Signing**

用于桌面发行签名流程。 [查看原始资料](https://www.electronjs.org/docs/latest/tutorial/code-signing)

**[S29] W3C：Understanding WCAG 2.2 / Contrast (Minimum)**

用于普通正文对比度目标，不表示本设计已完成无障碍认证。 [查看原始资料](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)

**[S30] Electron 官方教程：Updating Applications**

用于更新机制设计依据，企业离线策略为本项目建议。 [查看原始资料](https://www.electronjs.org/docs/latest/tutorial/updates)

**[S31] Chen：AuthController.java**

固定提交 d9890760dfdf；连接令牌交换 Chen 会话和 HTTP session 绑定。 [查看原始资料](https://github.com/jumpserver/chen/blob/d9890760dfdf87595bcaf6ca388fd96892f8f12f/backend/web/src/main/java/org/jumpserver/chen/web/controller/AuthController.java)

